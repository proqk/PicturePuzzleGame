using System.Collections;
using System.Collections.Generic;
using System.IO;
using UnityEngine;

public class test : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
        //string tmp = "31";
        //print(Path.Combine("symbol_Images/", tmp.ToString()+".png"));
        //print(File.Exists(Path.Combine("Assets/Resources/symbol_Images/", tmp.ToString()+".png")));
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
